import styled from 'styled-components';

const Purchases = styled.div`
  margin: 0 auto;
  max-width: 850px;
  display: flex;
  flex-direction: column;
  overflow: auto;
  gap: 40px;
`;

export { Purchases };
